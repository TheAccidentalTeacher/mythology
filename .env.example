# ================================================================
# 🕶️ MYTHOLOGY PROJECT - ENVIRONMENT VARIABLES
# ================================================================
# This file contains all API keys and configuration needed for the app.
# NEVER commit the actual .env file to git - keep your keys secret!
# Copy this file to .env and fill in your actual keys.
# ================================================================

# ----------------------------------------------------------------
# SUPABASE (Database & Authentication)
# ----------------------------------------------------------------
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
# 
# NEXT_PUBLIC_SUPABASE_URL: Your project URL (safe to expose in browser)
# NEXT_PUBLIC_SUPABASE_ANON_KEY: Public anon key (safe to expose, row-level security protects data)
# SUPABASE_SERVICE_ROLE_KEY: Admin key (NEVER expose in client-side code, server-only)

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------
# OPENAI API (AI Features)
# ----------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
#
# Used for:
# - Content moderation (detecting inappropriate content)
# - AI-generated worldbuilding prompts
# - Consistency checking
# - Text generation assistance
#
# Cost: ~$0.002 per moderation check, ~$0.01-0.03 per text generation

OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Specify model versions
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MODERATION_MODEL=text-moderation-latest

# ----------------------------------------------------------------
# IMAGE GENERATION (AI Art)
# ----------------------------------------------------------------
# Choose ONE of these services for AI image generation:
#
# Option 1: OpenAI DALL-E 3 (same key as above, ~$0.04 per image)
# Option 2: Stability AI (https://platform.stability.ai/)
# Option 3: Replicate (https://replicate.com/)

# For DALL-E (uses OPENAI_API_KEY above)
IMAGE_GENERATOR=dalle
DALLE_MODEL=dall-e-3
DALLE_SIZE=1024x1024

# For Stability AI
# STABILITY_API_KEY=sk-your-stability-key-here
# IMAGE_GENERATOR=stability

# For Replicate
# REPLICATE_API_TOKEN=your-replicate-token-here
# IMAGE_GENERATOR=replicate

# ----------------------------------------------------------------
# IMAGE MODERATION (Safety)
# ----------------------------------------------------------------
# For detecting inappropriate images uploaded by students
#
# Option 1: AWS Rekognition (requires AWS credentials)
# Option 2: Google Cloud Vision API
# Option 3: Azure Content Moderator

# AWS Rekognition (recommended)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Google Cloud Vision
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Azure Content Moderator
# AZURE_CONTENT_MODERATOR_KEY=your-azure-key
# AZURE_CONTENT_MODERATOR_ENDPOINT=https://your-region.api.cognitive.microsoft.com/

# ----------------------------------------------------------------
# APPLICATION SETTINGS
# ----------------------------------------------------------------

# Environment (development, staging, production)
NODE_ENV=development

# Next.js URL (for OAuth callbacks, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Maximum file upload size (in MB)
MAX_UPLOAD_SIZE_MB=10

# AI rate limiting (per student per day)
MAX_AI_GENERATIONS_PER_DAY=10
MAX_AI_TEXT_REQUESTS_PER_DAY=20

# Content moderation sensitivity (0.0 - 1.0, higher = stricter)
MODERATION_THRESHOLD=0.7

# ----------------------------------------------------------------
# EMAIL (Optional - for notifications)
# ----------------------------------------------------------------
# Used for sending teacher alerts about flagged content
#
# Option 1: SendGrid
# SENDGRID_API_KEY=your-sendgrid-key
# SENDGRID_FROM_EMAIL=notifications@yourdomain.com

# Option 2: Resend
# RESEND_API_KEY=your-resend-key
# RESEND_FROM_EMAIL=notifications@yourdomain.com

# Option 3: Use Supabase Auth email (free but limited)

# ----------------------------------------------------------------
# LOGGING & MONITORING (Optional)
# ----------------------------------------------------------------

# Sentry (error tracking)
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn

# LogRocket (session replay)
# LOGROCKET_APP_ID=your-logrocket-id

# ----------------------------------------------------------------
# DEPLOYMENT SETTINGS
# ----------------------------------------------------------------

# These are typically set by your hosting provider (Netlify, Vercel, etc.)
# and don't need to be in this file, but listed here for reference

# Netlify automatically provides:
# - URL
# - DEPLOY_URL
# - CONTEXT (production, deploy-preview, branch-deploy)

# Vercel automatically provides:
# - VERCEL_URL
# - VERCEL_ENV (production, preview, development)

# ----------------------------------------------------------------
# SECURITY KEYS (Generated by you)
# ----------------------------------------------------------------

# NextAuth.js secret (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000

# JWT signing key (if using custom JWT implementation)
# JWT_SECRET=your-jwt-secret-here

# ----------------------------------------------------------------
# FEATURE FLAGS (Optional)
# ----------------------------------------------------------------
# Toggle features on/off without code changes

# Enable AI image generation
FEATURE_AI_IMAGES=true

# Enable AI text generation
FEATURE_AI_TEXT=true

# Enable cross-mythology interactions
FEATURE_CROSSOVERS=false

# Enable public gallery (outside of classroom)
FEATURE_PUBLIC_GALLERY=false

# Enable parent read-only accounts
FEATURE_PARENT_ACCOUNTS=false

# ----------------------------------------------------------------
# NOTES FOR DEPLOYMENT
# ----------------------------------------------------------------
#
# NETLIFY:
# - Set these in: Site Settings > Build & Deploy > Environment Variables
# - Prefix client-side vars with NEXT_PUBLIC_
#
# VERCEL:
# - Set these in: Project Settings > Environment Variables
# - Can set different values per environment (production/preview/development)
#
# LOCAL DEVELOPMENT:
# - Copy this file to .env.local (git-ignored by default in Next.js)
# - Fill in actual values
# - Never commit .env.local to version control
#
# SECURITY REMINDERS:
# - NEVER commit actual API keys to git
# - Use service role keys ONLY in server-side code (API routes)
# - Rotate keys if accidentally exposed
# - Use different keys for dev/staging/production
# ----------------------------------------------------------------
